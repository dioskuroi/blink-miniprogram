<view class="container">
  <image class="classic-img {{playing ? 'play' : 'paused'}}" src="{{ img }}" />
  <image bind:tap="onPlay" class="player-img" src="{{ playing ? pauseSrc : playSrc }}" />
  <image class="tag" src="./images/music@tag.png" />
  <text class="content">{{ content }}</text>
</view>